/* exported setValue */

/*
pseudocode logic
----------------
setValue(object, key, value)

objective
----------------
assign a key value to an object

plan
----------------
add using bracket notation

*/

function setValue(object, key, value) {
  object[key] = value;
}
