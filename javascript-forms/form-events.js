/* function handleFocus(events) {
  console.log("'focus event fired' value :  ", 'focus event fired');
  console.log('event.target.name value :  ', event.target.name);
}
function handleBlur(event) {
  console.log("'blur event was fired' value :  ", 'blur event was fired');
  console.log('event.target.name value :  ', event.target.name);
}
function handleInput(event) {
  console.log('event.target.name value :  ', event.target.name);
  console.log('event.target.value value :  ', event.target.value);
}
var $name = document.querySelector('input#user-name');
var $email = document.querySelector('input#user-email');
var $textArea = document.querySelector('textarea');

$name.addEventListener('focus', handleFocus);
$name.addEventListener('blur', handleBlur);
$name.addEventListener('input', handleInput);

$email.addEventListener('focus', handleFocus);
$email.addEventListener('blur', handleBlur);
$email.addEventListener('input', handleInput);

$textArea.addEventListener('focus', handleFocus);
$textArea.addEventListener('blur', handleBlur);
$textArea.addEventListener('input', handleInput);
 */
