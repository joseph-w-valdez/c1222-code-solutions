/* exported last */

/*
pseudocode logic
----------------
last(array)

objective
---------------
extract the last value of the array and return it

plan
--------------
send array[array.length-1] and return it

*/

function last(array) {
  return array[array.length - 1];
}
